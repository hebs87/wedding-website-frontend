services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - ".:/app"
      - "/app/node_modules"
    command: vite
    ports:
      - "3000:3000"
    environment:
      - CHOKIDAR_USEPOLLING=true
